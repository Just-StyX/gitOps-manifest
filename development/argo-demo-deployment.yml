apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-demo-deployment
  namespace: argo-demo-namespace
  labels:
    app: argo-demo-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: argo-demo-deployment
  template:
    metadata:
      labels:
        app: argo-demo-deployment
    spec:
      containers:
        - name: argo
          image: ghcr.io/just-styx/argo-demo:1.3
          imagePullPolicy: Always
          ports:
            - containerPort: 8081

---
apiVersion: v1
kind: Service
metadata:
  name: argo-demo-service
  namespace: argo-demo-namespace
spec:
  ports:
    - port: 80
      targetPort: 8081
  selector:
    app: argo-demo-deployment